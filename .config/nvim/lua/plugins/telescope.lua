return {
    {
    -- bits for lazy to load the plugin
    'nvim-telescope/telescope.nvim',
    tag = '0.1.8',
    dependencies = { 'nvim-lua/plenary.nvim' },
    -- config settings
    config = function()
    end
    }
}
